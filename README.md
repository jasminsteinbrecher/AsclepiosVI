# AsclepiosVI
This repository holds the current instance of the YAMCS Software for Asclepios VI.

All config files should be modified in myproject\src\main\yamcs\etc (e.g., files like security.yaml).

To make a new executable run mvn clean package and replace the instance files in packaged.
